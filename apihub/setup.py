from setuptools import setup, find_packages
setup(
    name='apihub',
    version='0.0.1',
    author='Priya Ranjan',
    license='',
    packages=find_packages(),
    install_requires=[
        'stripe'
    ],
)